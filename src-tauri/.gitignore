# Generated by Cargo
# will have compiled files and executables
/target/
/gen/schemas

# Built gateway sidecar artifact (scripts/build-sidecar.mjs) — large (bundled
# node + native node_modules); rebuild with `npm run build:sidecar`.
/binaries/
