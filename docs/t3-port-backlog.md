# t3-code → Conan feature-port backlog

Features from t3-code (reference source in `t3code/`, gitignored) worth porting
to Conan's chat surface. Derived from the t3-code interface map (2026-07-22).
Not yet decomposed into stories — this is the wishlist to prioritize, then feed
to `decompose-prd` (or build by hand).

**Status:** wishlist · queued · building · done · skip.
**Size:** S (<half a story) · M (a story) · L (multi-story / new subsystem).

## Already have (Conan's chat-v1 covers these)
- Sigil composer (`@` files/folders, `$` skills, `/` commands), model +
  permission chips, interactive tool-approval, token streaming, tool cards,
  inline diffs, plan cards, thread sidebar + persistence + resume, activity
  spine (Conan-only — t3 has no timeline).

## Port candidates

| ID | Feature (t3-code) | Notes for Conan | Size | Status |
|----|-------------------|-----------------|------|--------|
| T3-1 | **Multi-provider** (Codex / Cursor / OpenCode / Grok) | The `AgentDriver` seam (`src/agent/driver.ts`) already exists for this, but t3's version is a full provider subsystem: driver SPI, provider instances, per-provider adapters/runtimes, settings, model picker, and status/update handling. Lock provider per-thread after first turn, as t3 does. Hidden deps: provider-instance schema, per-thread provider identity, event normalization per provider, settings UI, install/update probes. Evidence: `t3code/apps/server/src/provider/ProviderDriver.ts`, `t3code/apps/server/src/provider/Drivers/{ClaudeDriver,CodexDriver,CursorDriver,OpenCodeDriver,GrokDriver}.ts`, `t3code/apps/server/src/provider/Layers/*Provider.ts`, `t3code/apps/web/src/components/chat/ProviderModelPicker.tsx`, `t3code/apps/web/src/components/settings/AddProviderInstanceDialog.tsx`. | L | **DONE 2026-07-23** (branch `loop/conan-multi-provider`): Claude/Codex/Grok behind the AgentDriver seam — capabilities descriptor, registry + install probe, per-thread provider, capability-driven UI. Honest limits + one open bug in `docs/multi-provider-qa.md` (codex/grok reopen loses context; grok reopen exits 1 on saved model). Natural follow-on: **T3-19 provider maintenance UI**. |
| T3-2 | **Image attachments** | Paste/drop images into the composer (t3 persists drafts, enforces max count/bytes, uploads/stores assets, and passes provider-specific image inputs). Claude supports image content blocks, but Conan needs composer state, attachment persistence, upload/storage route or WS payload design, transcript replay, and driver serialization. Evidence: `t3code/apps/web/src/composerDraftStore.ts`, `t3code/apps/web/src/components/chat/ChatComposer.tsx`, `t3code/apps/server/src/attachmentStore.ts`, `t3code/apps/server/src/attachmentPaths.ts`, `t3code/apps/server/src/textGeneration/{ClaudeTextGeneration,CodexTextGeneration,OpenCodeTextGeneration}.ts`, `t3code/packages/contracts/src/provider.ts`. | L | wishlist |
| T3-3 | **@-mention content pinning** | Today Conan `@` inserts a path reference for the agent to read. t3 still serializes file mentions as inline chips, but also has richer pending context channels (terminal context, element context, preview annotations, review comments) that append structured context blocks. Port as "real context attachments/pins", not just prettier chips. Hidden deps: context serialization in outgoing turns, transcript rendering, and persistence. Evidence: `t3code/apps/web/src/components/ComposerPromptEditor.tsx`, `t3code/apps/web/src/components/chat/ChatComposer.tsx`, `t3code/apps/web/src/lib/terminalContext.ts`, `t3code/apps/web/src/lib/elementContext.ts`, `t3code/apps/web/src/reviewCommentContext.ts`. | M | wishlist |
| T3-4 | **Full diff panel / viewer** | t3 routes diffs to a dedicated Diff surface with split/stacked rendering, word wrap, ignore-whitespace, base-ref selection, file actions, and annotatable code. Conan shows inline diffs in tool cards; a serious port is a side/fullscreen viewer plus backend diff summaries, not a small modal. Evidence: `t3code/apps/web/src/components/DiffPanel.tsx`, `t3code/apps/web/src/components/DiffPanelShell.tsx`, `t3code/apps/web/src/components/diffs/AnnotatableCodeView.tsx`, `t3code/apps/web/src/diffPanelStore.ts`, `t3code/apps/web/src/hooks/useTurnDiffSummaries.ts`, `t3code/apps/web/src/diffFileActions.ts`. | L | wishlist |
| T3-5 | **Context-window meter** | t3's composer gauge reads provider context-window events. Conan can beat it by showing cost too, but the source feature depends on normalized `context-window.updated` activity and a provider context snapshot, not just a UI ring. Evidence: `t3code/apps/web/src/components/chat/ContextWindowMeter.tsx`, `t3code/apps/web/src/components/chat/ChatComposer.tsx`, `t3code/apps/web/src/lib/contextWindow.ts`, `t3code/apps/server/src/orchestration/Layers/ProviderRuntimeIngestion.ts`, `t3code/apps/web/src/session-logic.ts`. | M | wishlist |
| T3-6 | **Reasoning-effort / traits controls** | t3 exposes provider-specific trait controls through the composer and persists them per provider/model. For Conan this maps to model params or prompt prefixes (Claude "ultrathink" etc.). Note: reasoning TEXT is redacted headless (see HANDOFF D2), but effort still affects behavior. Hidden dep: provider-specific option schema so controls don't become hard-coded Claude-only UI. Evidence: `t3code/apps/web/src/components/chat/TraitsPicker.tsx`, `t3code/apps/web/src/components/chat/composerProviderState.tsx`, `t3code/apps/web/src/composerDraftStore.ts`, `t3code/apps/web/src/providerModels.ts`, `t3code/apps/web/src/modelSelection.ts`. | M | wishlist |
| T3-7 | **Worktree / "environment" isolation** | t3 lets a thread run in the current checkout or a dedicated git worktree/branch, with PR-specific prep and cleanup affordances. Conan uses a shared cwd per project. This is a real subsystem: create/track/clean worktrees, store `worktreePath` on threads, run setup scripts, and decide how resume maps to cwd. Evidence: `t3code/apps/web/src/components/BranchToolbar.tsx`, `t3code/apps/web/src/components/BranchToolbar.logic.ts`, `t3code/apps/web/src/worktreeCleanup.ts`, `t3code/apps/server/src/git/GitManager.ts`, `t3code/apps/server/src/git/GitWorkflowService.ts`, `t3code/packages/contracts/src/git.ts`, `t3code/packages/contracts/src/settings.ts`. | L | wishlist |
| T3-8 | **Remote project sources** (Git URL / GitHub / GitLab / Bitbucket / Azure clone) | t3's source-control layer goes beyond "clone a URL": it discovers providers, checks auth, lists/creates repositories, and clones/publishes through provider-neutral APIs. This is the deferred backlog item A1-b but bigger than a picker. Hidden deps: CLI auth probes, progress/errors, credential boundaries, and provider-specific remote URL handling. Evidence: `t3code/apps/server/src/sourceControl/SourceControlRepositoryService.ts`, `t3code/apps/server/src/sourceControl/SourceControlProviderRegistry.ts`, `t3code/apps/server/src/sourceControl/{GitHubSourceControlProvider,GitLabSourceControlProvider,BitbucketSourceControlProvider,AzureDevOpsSourceControlProvider}.ts`, `t3code/apps/web/src/components/settings/SourceControlSettings.tsx`. | L | wishlist |
| T3-9 | **Agent free-form questions panel** | t3 has a distinct pending-user-input composer panel separate from tool approval. Conan currently routes user-input-like events through notifications only. Needs agent event support, pending state, answer submission, transcript rendering, and stale-request handling. Evidence: `t3code/apps/web/src/components/chat/ComposerPendingUserInputPanel.tsx`, `t3code/apps/web/src/components/chat/ChatComposer.tsx`, `t3code/apps/web/src/pendingUserInput.ts`, `t3code/apps/web/src/session-logic.ts`, `t3code/apps/server/src/orchestration/decider.ts`, `t3code/apps/server/src/orchestration/Layers/ProviderCommandReactor.ts`. | M | wishlist |
| T3-10 | **Right-panel work surfaces** (Browser / Terminal / Files / Diff / Plan) | t3's live observation tabs sit beside the thread with multiple surface types and tab management. Conan deliberately cut panels; revisit only if one surface earns its space. Files/Diff are the strongest candidates; Terminal/Browser/Plan are likely product creep unless tied to a concrete workflow. Evidence: `t3code/apps/web/src/components/RightPanelTabs.tsx`, `t3code/apps/web/src/rightPanelStore.ts`, `t3code/apps/web/src/rightPanelLayout.ts`, `t3code/apps/web/src/components/RightPanelSheet.tsx`, `t3code/apps/web/src/components/ThreadTerminalDrawer.tsx`, `t3code/apps/web/src/components/files/FileBrowserPanel.tsx`, `t3code/apps/web/src/components/preview/PreviewPanel.tsx`, `t3code/apps/web/src/components/PlanSidebar.tsx`. | L | skip |
| T3-11 | **Draft-based new-thread flow** | t3 creates a local draft session that promotes to a real server thread on first send and reuses one empty draft per project. Conan creates the thread immediately. This is a minor UX/data-model change, but hidden dep is preserving model/permission/context draft state before a server thread exists. Evidence: `t3code/apps/web/src/composerDraftStore.ts`, `t3code/apps/web/src/routes/_chat.index.tsx`, `t3code/apps/web/src/routes/_chat.draft.$draftId.tsx`, `t3code/apps/web/src/routes/_chat.$environmentId.$threadId.tsx`, `t3code/apps/web/src/threadRoutes.ts`. | M | wishlist |
| T3-12 | **Remote access** (SSH / Tailscale / relay / `npx t3` web server) | t3 is web/remote-first with first-class environments, hosted pairing, SSH launch helpers, Tailscale endpoints, and relay infra. Conan is a local native app around a loopback sidecar; this is a different product direction unless Conan intentionally becomes multi-device/remote. Evidence: `t3code/docs/architecture/remote.md`, `t3code/packages/ssh/src/{auth,command,config,tunnel}.ts`, `t3code/packages/tailscale/src/tailscale.ts`, `t3code/infra/relay/src/worker.ts`, `t3code/apps/web/src/components/cloud/*`, `t3code/apps/web/src/routes/pair.tsx`. | L | skip |
| T3-13 | **Source-control / PR workflows** | t3 has first-class branch status/actions, changed-file trees, create PR/MR, open PR thread, and local-vs-worktree PR prep. This is adjacent to T3-7/T3-8 but user-facing enough to track separately. Hidden deps: Git backend, provider auth, thread-to-branch metadata, and conflict/error UX. Evidence: `t3code/apps/web/src/components/GitActionsControl.tsx`, `t3code/apps/web/src/components/BranchToolbar.tsx`, `t3code/apps/web/src/components/PullRequestThreadDialog.tsx`, `t3code/apps/web/src/components/chat/ChangedFilesTree.tsx`, `t3code/apps/server/src/git/GitManager.ts`, `t3code/apps/server/src/sourceControl/SourceControlProvider.ts`. | L | wishlist |
| T3-14 | **Project scripts / setup actions** | t3 lets projects define runnable scripts with icons, keybindings, run-on-worktree-create, preview URL, and auto-open preview. Conan has the autonomous `run-tasks.sh` loop but no per-project action toolbar. Good local-native feature if kept to project scripts, not full t3 workflow automation. Hidden deps: project schema, script execution route, output/terminal destination, and keybinding integration. Evidence: `t3code/apps/web/src/components/ProjectScriptsControl.tsx`, `t3code/apps/web/src/projectScripts.ts`, `t3code/apps/web/src/lib/projectScriptKeybindings.ts`, `t3code/packages/contracts/src/orchestration.ts`, `t3code/apps/server/src/orchestration/decider.ts`. | M | wishlist |
| T3-15 | **Preview browser + agent element context** | t3's desktop browser panel discovers local servers, opens URLs, supports automation hosts, and can capture selected DOM/React element context back into the composer. Conan should not port the whole browser unless it wants right panels again, but the element-context idea could be valuable for frontend debugging. Hidden deps: desktop/webview browser bridge, automation protocol, context serialization, and asset screenshots if added. Evidence: `t3code/apps/web/src/components/preview/PreviewPanel.tsx`, `t3code/apps/web/src/components/preview/PreviewView.tsx`, `t3code/apps/web/src/components/preview/useDiscoveredLocalServers.ts`, `t3code/apps/web/src/components/preview/previewAutomationRequestConsumer.ts`, `t3code/apps/web/src/lib/elementContext.ts`, `t3code/apps/web/src/components/chat/ComposerPendingElementContexts.tsx`. | L | skip |
| T3-16 | **File browser / file viewer with chat handoff** | t3's Files surface can browse/search a project tree, drag/right-click paths into chat, open file preview tabs, save edits, and attach annotations. Conan already has `@` path autocomplete but not a persistent file surface. If ported, scope it to file browsing + add-to-chat, not an editor clone. Evidence: `t3code/apps/web/src/components/files/FileBrowserPanel.tsx`, `t3code/apps/web/src/components/files/FilePreviewPanel.tsx`, `t3code/apps/web/src/components/files/fileTreeDragMention.ts`, `t3code/apps/web/src/components/files/fileSaveCoordinator.ts`, `t3code/apps/web/src/components/files/LocalCommentAnnotation.tsx`, `t3code/apps/web/src/components/chat/ComposerPendingReviewComments.tsx`. | M | wishlist |
| T3-17 | **Custom keybindings** | t3 has a JSON-backed keybinding system with command IDs, `when` expressions, settings UI, conflict/parse errors, and live config updates. Conan has fixed shortcuts only. Hidden deps: persisted config file or DB table, command registry, conflict handling, and settings UX. Evidence: `t3code/packages/contracts/src/keybindings.ts`, `t3code/apps/server/src/keybindings.ts`, `t3code/apps/web/src/keybindings.ts`, `t3code/apps/web/src/components/settings/KeybindingsSettings.tsx`, `t3code/apps/web/src/routes/_chat.tsx`, `t3code/apps/web/src/routes/settings.keybindings.tsx`. | M | wishlist |
| T3-18 | **External editor / "open in" integrations** | t3 detects installed editors and can open the workspace/path in VS Code, Cursor, Zed, JetBrains, or file manager. Useful for Conan because it is desktop-local and does not require t3's remote architecture. Hidden dep: native/sidecar process launcher and preferred-editor setting. Evidence: `t3code/packages/contracts/src/editor.ts`, `t3code/apps/server/src/process/externalLauncher.ts`, `t3code/apps/web/src/components/chat/OpenInPicker.tsx`, `t3code/apps/web/src/editorPreferences.ts`, `t3code/apps/web/src/markdown-links.ts`, `t3code/apps/web/src/terminal-links.ts`. | S | wishlist |
| T3-19 | **Provider install/update maintenance UI** | t3 detects provider versions, compares latest package versions, surfaces update notifications, and can launch update commands with locking. This should probably ride behind T3-1, not ship standalone. Hidden deps: provider registry, command execution with progress, package-manager detection, and settings/status cards. Evidence: `t3code/apps/server/src/provider/providerMaintenance.ts`, `t3code/apps/server/src/provider/providerMaintenanceRunner.ts`, `t3code/apps/web/src/components/ProviderUpdateLaunchNotification.tsx`, `t3code/apps/web/src/components/ProviderUpdatePrimaryNotification.tsx`, `t3code/apps/web/src/components/settings/ProviderInstanceCard.tsx`, `t3code/apps/web/src/components/settings/providerStatus.ts`. | M | wishlist |
| T3-20 | **Archived threads/projects** | t3 models archived state and has an archived settings route; Conan currently has delete and persisted threads, but not archive/restore. Small product-quality feature if thread history becomes a Premium or power-user surface. Hidden dep: DB schema and sidebar filters/counts. Evidence: `t3code/apps/web/src/routes/settings.archived.tsx`, `t3code/apps/web/src/components/settings/SettingsPanels.tsx`, `t3code/apps/server/src/orchestration/decider.ts`, `t3code/apps/server/src/orchestration/commandInvariants.ts`, `t3code/apps/server/src/orchestration/Layers/ProjectionSnapshotQuery.ts`. | M | wishlist |
| T3-21 | **Mobile clients / T3 Connect pairing** | t3 includes mobile/web pairing and managed auth surfaces. This is remote/multi-device product direction, not a local desktop Claude cockpit feature. Evidence: `t3code/apps/mobile/src/widgets/AgentActivity.tsx`, `t3code/apps/web/src/routes/connect.tsx`, `t3code/apps/web/src/routes/connect_.callback.tsx`, `t3code/apps/web/src/components/clerk/T3ConnectSidebarSignIn.tsx`, `t3code/apps/web/src/components/cloud/ConnectOnboardingDialog.tsx`, `t3code/docs/cloud/t3-connect-clerk.md`. | L | skip |

## Suggested first slice
~~If porting: **T3-1 multi-provider** is the highest-leverage~~ — **T3-1
shipped 2026-07-23** (see its row above and `docs/multi-provider-qa.md`).
**T3-19 provider maintenance** is the natural follow-on now that the registry
+ install probe exist. Smaller slices now
look more like M once backend/persistence is counted: **T3-18 external editor
open-in (S)** is the only true quick win; then **T3-5 context+cost meter (M)**,
**T3-11 draft threads (M)**, **T3-6 effort control (M)**, and **T3-3 content
pinning (M)**.
