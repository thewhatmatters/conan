# WHA-142 — C2: Overview (default) + Needs you from `decision.*`

- **status:** verify-pending
- **builder_id:** codex-gpt-5.6-sol
- **verifier_id:** hermes-pm
- **evidence_sha:** 3ac1983
- **linear:** https://linear.app/whatmatters/issue/WHA-142/c2-overview-default-needs-you-from-decision
- **owner:** Booker (implementation)
- **depends_on:** B2, C1

## AC

1. Default tab is Overview.
2. Sections: Needs you, Running now, Up next, Blocked, Recently completed (exact labels up to builder).
3. Row shows task/lane, assigned agent/role, state by icon + label (not color alone), and duration.
4. "Needs you" count appears in Sagan tab entry and run header.
5. Loading, empty, error, malformed-config states designed and reachable.
6. Full keyboard traversal and visible focus.
7. Verify against `thewhatmatters/sagan` @ `ab2cb8d`: WHA-130 appears in Needs you; T-001 does not; WHA-130's prior resolved round does not false-close it.

## Non-goals

- Promote-gate action UI (C5).
- Pipeline view (C4).

## Frontend

- Built the C2 Overview in `V2SaganSurface` and made it the Sagan surface's
  default/only C2 pane. The shell now fetches `/api/sagan/runs` once per `cwd`,
  shares the full result with the mounted surface, and derives the existing
  Sagan tab's Needs-you count from that response.
- Classification is exclusive and ordered: unanswered `openDecisions` → Needs
  you; blocking phase or `ESCALATE`/`REVISE`/`NEEDS_EVIDENCE` → Blocked;
  `done`/`merged`/completed → Recently completed; queued/backlog/pending → Up
  next; remaining active lanes → Running now. Rows expose task/lane,
  agent/role, icon + state text, and ledger-span duration. Row buttons use the
  existing StyleX focus token; no C3 click behavior was added.
- Designed loading, empty, request-error, invalid/unsupported manifest, absent,
  and malformed-payload states. Invalid overlays remain available in the
  surface picker so their configuration error is reachable.
- Fixture check: read `src/sagan/api.test.ts` / `src/sagan/ledger.test.ts`, which
  mirror `thewhatmatters/sagan@ab2cb8d`. The projection returns Needs you =
  `{WHA-130}`: its resolved earlier round is superseded by the round-3
  `decision.needed`; T-001's last promote event is `decision.made`, so it is
  excluded. The new surface regression test renders that exact inclusion.
- Gates (Node 22): root typecheck green; UI Vitest 399/399 green; UI production
  build green. Root `npm test`: 349 passed, 13 failed only because sandboxed
  live-server tests in `src/browser/{mcp,probe,read}.test.ts` cannot bind
  `127.0.0.1` (`listen EPERM`). Focused C2/workspace tests: 11/11 green.

## QA

(verify appends here)

## Decisions
