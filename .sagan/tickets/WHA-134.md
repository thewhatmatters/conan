# WHA-134 — Sagan enforced tier v1 (runtime code for caps, isolation, gates, containment floors)

- **status:** canceled
- **builder_id:** (set at dispatch)
- **verifier_id:** (set at verify)
- **evidence_sha:** (set by verify)
- **linear:** https://linear.app/whatmatters/issue/WHA-134/sagan-enforced-tier-v1-runtime-code-for-caps-isolation-gates
- **owner:** TBD

## AC

(To be written from the locked PRD before any dispatch. Placeholder until WHA-133 completes. Known invariant: nothing safety-critical may depend on a model remembering to stop.)

## Blocked reason

PRD WHA-133 is canceled. AC cannot be written without a locked PRD. Not starting in this run.

## Frontend

(builder appends here)

## QA

(verify appends here)

## Decisions

- 2026-08-08: PM (Hermes) blocked this ticket from the Sagan run pending a new/revived PRD decision.
- 2026-08-10: Canceled by Randy — superseded by the WHA-122 track in the Conan
  Linear project, which built this scope while the PRD stayed cancelled.
  `src/fleet/{containment,dispatch,attempts}.ts` are on `main-v2`, and
  `docs/conan-fleet-phase0-freeze.md` was accepted as an as-built ADR the same
  day (PR #60). Remaining scope: WHA-126 (trust predicates, incl. the
  `same_finding_failures` cap) and WHA-125 (lineage schema).
- 2026-08-10: **Build-vs-adopt recorded rather than lost.** The substrate
  question this ticket was holding — build in-house, or adopt Bernstein
  (`bernstein.run`: deterministic Python orchestration, 40+ CLI adapters,
  worktree isolation, HMAC audit logs) — was never evaluated. In-house won by
  default: `src/fleet/` was written while the question sat behind a cancelled
  PRD, and the ADR then ratified what already existed. Possibly the right
  answer; not a considered one. The honest framing is "we chose by building."
- 2026-08-10: This file was **untracked in git** until now, so the 08-08 block
  reason lived on one machine while Linear showed an unexplained Backlog.
  Committing it is the point of this change.
