# WHA-141 — C1: Register `"sagan"` as a dockable surface

- **status:** building
- **builder_id:** codex-gpt-5.6-sol
- **verifier_id:** (set at verify)
- **evidence_sha:** (set by verify)
- **linear:** https://linear.app/whatmatters/issue/WHA-141/c1-register-sagan-as-a-dockable-surface
- **owner:** Booker (implementation)
- **depends_on:** B1

## AC

1. Add `"sagan"` to `SurfaceId` (`SurfaceTabs.tsx:31`), `SURFACE_OPTIONS`, and `V2SurfaceBodies.tsx`.
2. Docking works left/right.
3. Tab/icon/title present only when marker is valid; zero residual chrome after switching to a non-Sagan project.
4. Surface stays mounted across chat-thread changes.
5. Human `/ws/agent` socket stays up while Sagan is open and the human switches chat threads.
6. Verify against `thewhatmatters/sagan` @ `ab2cb8d` — Sagan tab appears.

## Non-goals

- Overview content (C2).
- Pipeline view (C4).
- Gate actions (C5).

## Frontend

- Registered `sagan` as an Orbit-icon surface, gated its Surface-menu option on
  `GET /api/sagan/runs?projectId=…` reporting `project.sagan.state === "valid"`,
  and wired the existing tab/left/right dock lifecycle. AppV2 clears the open
  tab, placement, and active-dock state on a non-Sagan project switch. The
  placeholder surface keeps its stable surface key across chat-thread changes,
  refetches on cwd changes, and provides loading/error/connected states.
- Regression coverage: conditional menu capability, zero residual Sagan chrome
  after switching projects, and stable mount/refetch behavior.
- Verification target: `thewhatmatters/sagan` @ `ab2cb8d` has a valid marker and
  should show the Sagan tab; live headless confirmation is deferred to PM QA as
  requested.
- Node 22 gates: `npm run typecheck` passed; `cd ui && npm run build` passed.
  Hermes re-ran the full gates with Node 22: root `npm test` 362/362 pass;
  focused `ui/src/v2` tests pass as part of the UI suite (393/393 overall).

## QA

(verify appends here)

## Decisions
