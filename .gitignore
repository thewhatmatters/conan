# dependencies
node_modules/
ui/node_modules/

# build output
dist/
ui/dist/
# typescript incremental build cache (machine-local; never version it)
*.tsbuildinfo

# local data (sqlite db, logs)
.data/
# git worktrees minted for isolated driven sessions (US-043)
.conan-worktrees/
# Claude Code worktrees (isolated agent sessions)
.claude/worktrees/
*.db
*.db-shm
*.db-wal
*.log

# env
.env
.env.local

# os / editor
.DS_Store
*.swp
.vscode/
.idea/

# build-loop run state
progress.txt
archive/

# Local planning/handoff scratch (read-once, not versioned)
# NOTE: HANDOFF.md is now TRACKED — it's the durable cross-agent handoff
# (read by Claude AND Codex/other agents), so it must travel with the repo.
.claude/current-task.txt
.claude/handoff-archive/
t3code/
loop.log

# wire-sagan commit policy: audit trail committed, evidence media ignored
.sagan/ledger/*/
!.sagan/ledger/events.jsonl
.sagan/memory/
